---
phase: 02-form-controls
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - package.json
  - index.js
autonomous: false
requirements: [FORM-01, FORM-02, FORM-03, FORM-04, FORM-05, FORM-06, UTIL-01]

must_haves:
  truths:
    - "Consumer can import any component via granular path: import 'td-components/toggle'"
    - "Consumer can import all components via main entry: import 'td-components'"
    - "Consumer can import DateTime utility via: import 'td-components/datetime'"
    - "Storybook builds successfully with all component stories"
  artifacts:
    - path: "package.json"
      provides: "Exports map for granular imports"
      contains: "./toggle"
    - path: "index.js"
      provides: "Main entry re-exporting all components"
      contains: "td-toggle"
  key_links:
    - from: "package.json exports"
      to: "src/form/td-toggle.js"
      via: "./toggle export path"
      pattern: "td-toggle"
    - from: "index.js"
      to: "src/form/*.js"
      via: "re-export statements"
      pattern: "export.*from.*src/form"
---

<objective>
Wire all Phase 2 components into package.json exports map and index.js, then verify Storybook builds successfully.

Purpose: Make all form components importable by consumers via granular paths (per DX-03) and verify the full Storybook with all stories.
Output: Updated package.json exports, updated index.js, passing Storybook build
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-form-controls/02-CONTEXT.md

<interfaces>
From package.json (current exports):
```json
"exports": {
  ".": "./index.js",
  "./base": "./src/base/td-base-element.js",
  "./sample": "./src/base/sample/td-sample.js"
}
```

From index.js (current):
```javascript
export { TdBaseElement } from './src/base/td-base-element.js';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add package.json exports and index.js re-exports</name>
  <files>package.json, index.js</files>
  <read_first>
    - /Users/theduyet/Documents/Code/components/package.json
    - /Users/theduyet/Documents/Code/components/index.js
  </read_first>
  <action>
    **package.json** — Add exports entries for each component (per DX-03 granular import pattern):
    ```json
    "exports": {
      ".": "./index.js",
      "./base": "./src/base/td-base-element.js",
      "./sample": "./src/base/sample/td-sample.js",
      "./toggle": "./src/form/td-toggle.js",
      "./checkbox": "./src/form/td-checkbox.js",
      "./button": "./src/form/td-button.js",
      "./input-field": "./src/form/td-input-field.js",
      "./slider": "./src/form/td-slider.js",
      "./dropdown": "./src/form/td-dropdown.js",
      "./datetime": "./src/utils/datetime.js"
    }
    ```

    **index.js** — Add re-exports for all new components and utility:
    ```javascript
    export { TdBaseElement } from './src/base/td-base-element.js';
    export { TdToggle } from './src/form/td-toggle.js';
    export { TdCheckbox } from './src/form/td-checkbox.js';
    export { TdButton } from './src/form/td-button.js';
    export { TdInputField } from './src/form/td-input-field.js';
    export { TdSlider } from './src/form/td-slider.js';
    export { TdDropdown } from './src/form/td-dropdown.js';
    export { TdDateTime } from './src/utils/datetime.js';
    ```
  </action>
  <verify>
    <automated>node -e "
      const pkg = require('./package.json');
      const fs = require('fs');
      const idx = fs.readFileSync('index.js','utf8');
      const exports = ['./toggle','./checkbox','./button','./input-field','./slider','./dropdown','./datetime'];
      const missing = exports.filter(e => !pkg.exports[e]);
      if (missing.length) { console.error('Missing exports:', missing); process.exit(1); }
      const reexports = ['TdToggle','TdCheckbox','TdButton','TdInputField','TdSlider','TdDropdown','TdDateTime'];
      const missingRe = reexports.filter(r => !idx.includes(r));
      if (missingRe.length) { console.error('Missing re-exports:', missingRe); process.exit(1); }
      console.log('All exports and re-exports present');
    "
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -q '"./toggle"' package.json
    - grep -q '"./checkbox"' package.json
    - grep -q '"./button"' package.json
    - grep -q '"./input-field"' package.json
    - grep -q '"./slider"' package.json
    - grep -q '"./dropdown"' package.json
    - grep -q '"./datetime"' package.json
    - grep -q "TdToggle" index.js
    - grep -q "TdCheckbox" index.js
    - grep -q "TdButton" index.js
    - grep -q "TdInputField" index.js
    - grep -q "TdSlider" index.js
    - grep -q "TdDropdown" index.js
    - grep -q "TdDateTime" index.js
  </acceptance_criteria>
  <done>package.json has 7 new export entries. index.js re-exports all 7 new symbols. Consumer can import via granular path or main entry.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Storybook renders all components</name>
  <files>none</files>
  <action>
    Run Storybook build to verify all stories compile, then launch dev server for user visual inspection.
    1. Run `npx storybook build --quiet` to verify compilation
    2. Run `npm run storybook` to launch dev server for user
  </action>
  <verify>
    <automated>npx storybook build --quiet 2>&1 | tail -5</automated>
  </verify>
  <done>Storybook builds without errors and user confirms all 6 form component stories render correctly with interactions working.</done>
</task>

</tasks>

<verification>
- package.json exports map includes all 7 new entries
- index.js re-exports all 7 new symbols
- Storybook builds/runs with all stories visible
- All components interactive in Storybook
</verification>

<success_criteria>
- Granular imports work: `import 'td-components/toggle'` resolves correctly
- Main import works: `import { TdToggle } from 'td-components'` resolves
- All Storybook stories render and are interactive
- User visually confirms components look correct
</success_criteria>

<output>
After completion, create `.planning/phases/02-form-controls/02-04-SUMMARY.md`
</output>
