---
phase: 04-display-stories
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/display/td-pagination.js
  - src/display/td-pagination.stories.js
  - src/display/td-empty-state.js
  - src/display/td-empty-state.stories.js
autonomous: true
requirements: [DISP-03, DISP-04]

must_haves:
  truths:
    - "Pagination renders page numbers with ellipsis for large page counts"
    - "Pagination highlights active page with custom color"
    - "Pagination shows info text (Hien thi X-Y / Z muc)"
    - "Empty State renders icon, title, message in a dashed-border card"
    - "Empty State supports 3 sizes (sm, md, lg) and compact mode"
    - "Empty State supports action buttons via JS property"
  artifacts:
    - path: "src/display/td-pagination.js"
      provides: "Standalone pagination web component"
      exports: ["TdPagination"]
      contains: "class TdPagination extends TdBaseElement"
    - path: "src/display/td-empty-state.js"
      provides: "Empty state web component"
      exports: ["TdEmptyState"]
      contains: "class TdEmptyState extends TdBaseElement"
    - path: "src/display/td-pagination.stories.js"
      provides: "Storybook stories for pagination"
      contains: "title: 'Display/Pagination'"
    - path: "src/display/td-empty-state.stories.js"
      provides: "Storybook stories for empty state"
      contains: "title: 'Display/EmptyState'"
  key_links:
    - from: "src/display/td-pagination.js"
      to: "src/base/td-base-element.js"
      via: "extends TdBaseElement"
      pattern: "extends TdBaseElement"
    - from: "src/display/td-empty-state.js"
      to: "src/base/td-base-element.js"
      via: "extends TdBaseElement"
      pattern: "extends TdBaseElement"
---

<objective>
Create td-pagination and td-empty-state Web Components ported from DCMS originals, with Storybook stories.

Purpose: Pagination is needed standalone and will be used by td-table (Plan 03). Empty State is used in tables and standalone.
Output: Two working web components with stories, following established TdBaseElement patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-display-stories/04-CONTEXT.md
@src/base/td-base-element.js
@src/form/td-dropdown.js (reference for JS property pattern, attributeChangedCallback override)
@src/form/td-button.stories.js (reference for story pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create td-pagination component</name>
  <files>src/display/td-pagination.js, src/display/td-pagination.stories.js</files>
  <read_first>
    - /Users/theduyet/Documents/Code/dcms2/resources/js/components/dcms-pagination.js (source to port)
    - /Users/theduyet/Documents/Code/components/src/base/td-base-element.js (base class)
    - /Users/theduyet/Documents/Code/components/src/form/td-button.stories.js (story pattern)
  </read_first>
  <action>
    Port dcms-pagination.js (PaginationSimple) to a Web Component extending TdBaseElement.

    **Component: td-pagination** (per D-03 — standalone, usable independently)

    Attributes:
    - `total-items` (number, default 0)
    - `items-per-page` (number, default 10)
    - `current-page` (number, 1-based, default 1)
    - `active-color` (CSS color, default '#ef4444')
    - `item-label` (string, default 'muc')
    - `max-pages` (number of page buttons to show, default 5)

    Behavior (port from DCMS):
    - Show info text: "Hien thi {start}-{end} / {total} {itemLabel}"
    - Prev/Next buttons with chevron SVG icons (do NOT use Font Awesome — use inline SVG like `<svg viewBox="0 0 20 20">` with chevron path). Disabled state when at first/last page.
    - Page number buttons with ellipsis (...) for large page counts. Port `_buildPageItems` logic exactly from dcms-pagination.js.
    - Active page styled with `active-color` attribute (text color + font-semibold).
    - Emit `page-change` event with `{ page }` detail on page click.
    - Override `attributeChangedCallback` to avoid full re-render on current-page change — just update the display.

    Glass styling: Match existing component glass card pattern. Page buttons use `hover:bg-black/5` transition. Prev/next buttons have border `border-gray-300` with `rounded-md`.

    Register: `if (!customElements.get('td-pagination')) customElements.define('td-pagination', TdPagination);`

    **Story: td-pagination.stories.js**
    - Title: 'Display/Pagination'
    - Default story with controls for all attributes
    - ManyPages story: total-items=500, items-per-page=10 (shows ellipsis)
    - FewPages story: total-items=30, items-per-page=10 (no ellipsis)
    - CustomColor story: active-color="#8b5cf6"
    - Use `play` function or inline script in render to add page-change event listener that logs to console
  </action>
  <verify>
    <automated>cd /Users/theduyet/Documents/Code/components && node -e "import('./src/display/td-pagination.js').then(m => { console.log('TdPagination:', typeof m.TdPagination); console.log('extends:', Object.getPrototypeOf(m.TdPagination).name); })"</automated>
  </verify>
  <acceptance_criteria>
    - grep "class TdPagination extends TdBaseElement" src/display/td-pagination.js
    - grep "page-change" src/display/td-pagination.js (emits event)
    - grep "_buildPageItems" src/display/td-pagination.js (ellipsis logic)
    - grep "active-color" src/display/td-pagination.js (custom color attribute)
    - grep "total-items" src/display/td-pagination.js (attribute)
    - grep "title: 'Display/Pagination'" src/display/td-pagination.stories.js
    - At least 4 story exports in stories file
  </acceptance_criteria>
  <done>TdPagination renders page nav with ellipsis, info text, prev/next buttons, custom active color, emits page-change event. Stories show all variants.</done>
</task>

<task type="auto">
  <name>Task 2: Create td-empty-state component</name>
  <files>src/display/td-empty-state.js, src/display/td-empty-state.stories.js</files>
  <read_first>
    - /Users/theduyet/Documents/Code/dcms2/resources/js/components/dcms-empty-state.js (source to port)
    - /Users/theduyet/Documents/Code/components/src/base/td-base-element.js (base class)
  </read_first>
  <action>
    Port dcms-empty-state.js (EmptyState) to a Web Component extending TdBaseElement.

    **Component: td-empty-state** (per D-04)

    Attributes:
    - `icon` (SVG string or icon class, default 'inbox' — render a default inbox SVG inline)
    - `title` (string, default 'Khong co du lieu')
    - `message` (string, default 'Chua co muc nao duoc tao.')
    - `size` ('sm' | 'md' | 'lg', default 'md')
    - `compact` (boolean, less padding)

    JS Properties:
    - `actions` — array of `{label, variant?, onClick}` objects. Rendered as plain HTML buttons styled with Tailwind (NOT td-button dependency — keep self-contained per Phase 3 pattern). Example: `el.actions = [{label: 'Tao moi', variant: 'primary', onClick: () => {}}]`

    Size mapping (port from DCMS):
    - sm: icon 28px, title text-base, message text-xs, gap 8px, padding 16px
    - md: icon 40px, title text-lg, message text-sm, gap 10px, padding 22px
    - lg: icon 56px, title text-xl, message text-base, gap 12px, padding 28px
    - compact: halves the padding

    Styling: Dashed border (`border-dashed`), rounded-xl, semi-transparent bg `rgba(255,255,255,0.6)`, centered flex column layout. Match DCMS glass card styling.

    Default icon: Render an inline SVG inbox icon (no Font Awesome dependency). Something like:
    ```
    <svg class="..." viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
      <path d="M20 ...inbox path..."/>
    </svg>
    ```

    Register: `if (!customElements.get('td-empty-state')) customElements.define('td-empty-state', TdEmptyState);`

    **Story: td-empty-state.stories.js**
    - Title: 'Display/EmptyState'
    - Default story with controls for title, message, size, compact
    - AllSizes story: sm, md, lg side by side
    - WithActions story: Use play function to set actions property with 2 buttons
    - Compact story: compact=true
  </action>
  <verify>
    <automated>cd /Users/theduyet/Documents/Code/components && node -e "import('./src/display/td-empty-state.js').then(m => { console.log('TdEmptyState:', typeof m.TdEmptyState); console.log('extends:', Object.getPrototypeOf(m.TdEmptyState).name); })"</automated>
  </verify>
  <acceptance_criteria>
    - grep "class TdEmptyState extends TdBaseElement" src/display/td-empty-state.js
    - grep "set actions" src/display/td-empty-state.js (JS property for action buttons)
    - grep "size" src/display/td-empty-state.js (3 sizes)
    - grep "compact" src/display/td-empty-state.js (boolean attribute)
    - grep "border-dashed" src/display/td-empty-state.js (dashed border styling)
    - grep "title: 'Display/EmptyState'" src/display/td-empty-state.stories.js
    - At least 3 story exports in stories file
  </acceptance_criteria>
  <done>TdEmptyState renders icon, title, message in dashed-border card. Supports 3 sizes, compact mode, action buttons via JS property. Stories show all variants.</done>
</task>

</tasks>

<verification>
- Both components import and extend TdBaseElement correctly
- Both register with customElements.define with collision check
- Stories follow CSF format matching existing form component stories
- No Font Awesome dependency — use inline SVG for icons
</verification>

<success_criteria>
- td-pagination renders page nav with ellipsis, info text, custom active color
- td-empty-state renders icon/title/message with 3 sizes and action buttons
- Both have Storybook stories with controls and multiple variants
- Components are self-contained (no external icon dependencies)
</success_criteria>

<output>
After completion, create `.planning/phases/04-display-stories/04-01-SUMMARY.md`
</output>
