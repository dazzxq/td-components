---
phase: 04-display-stories
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - src/feedback/td-modal.stories.js
  - src/feedback/td-toast.stories.js
  - src/feedback/td-tooltip.stories.js
  - src/feedback/td-loading.stories.js
  - package.json
  - index.js
autonomous: true
requirements: [DX-02]

must_haves:
  truths:
    - "Every component in the library has a Storybook story"
    - "Feedback stories show Modal variants, Toast 4 types, Tooltip positions, Loading fullscreen+inline"
    - "At least one composition story combines multiple components"
    - "Package exports include all display components"
    - "index.js re-exports all display components"
  artifacts:
    - path: "src/feedback/td-modal.stories.js"
      provides: "Modal stories"
      contains: "title: 'Feedback/Modal'"
    - path: "src/feedback/td-toast.stories.js"
      provides: "Toast stories"
      contains: "title: 'Feedback/Toast'"
    - path: "src/feedback/td-tooltip.stories.js"
      provides: "Tooltip stories"
      contains: "title: 'Feedback/Tooltip'"
    - path: "src/feedback/td-loading.stories.js"
      provides: "Loading stories"
      contains: "title: 'Feedback/Loading'"
    - path: "package.json"
      provides: "Package exports for display components"
      contains: "./table"
    - path: "index.js"
      provides: "Barrel re-exports for display components"
      contains: "TdTable"
  key_links:
    - from: "package.json"
      to: "src/display/td-table.js"
      via: "exports mapping"
      pattern: "./table.*td-table"
    - from: "index.js"
      to: "src/display/"
      via: "re-exports"
      pattern: "export.*from.*display"
---

<objective>
Write Storybook stories for all 4 feedback components (Modal, Toast, Tooltip, Loading) and wire package exports for all display components. Complete DX-02 coverage.

Purpose: Final phase — ensure every component has stories and the package is fully wired for consumers.
Output: 4 feedback story files, updated package.json exports, updated index.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-display-stories/04-CONTEXT.md
@src/feedback/td-modal.js
@src/feedback/td-toast.js
@src/feedback/td-tooltip.js
@src/feedback/td-loading.js
@src/form/td-button.stories.js (story pattern reference)
@package.json
@index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create feedback component stories</name>
  <files>src/feedback/td-modal.stories.js, src/feedback/td-toast.stories.js, src/feedback/td-tooltip.stories.js, src/feedback/td-loading.stories.js</files>
  <read_first>
    - /Users/theduyet/Documents/Code/components/src/feedback/td-modal.js (full file — understand API)
    - /Users/theduyet/Documents/Code/components/src/feedback/td-toast.js (full file)
    - /Users/theduyet/Documents/Code/components/src/feedback/td-tooltip.js (full file)
    - /Users/theduyet/Documents/Code/components/src/feedback/td-loading.js (full file)
    - /Users/theduyet/Documents/Code/components/src/form/td-button.stories.js (story pattern)
  </read_first>
  <action>
    Create Storybook stories for all 4 feedback components (per D-05 — at least 3 story variants each).

    **td-modal.stories.js** — Title: 'Feedback/Modal'
    - These are static-class modals (TdModal.show(), TdModal.confirm()), NOT custom elements. Stories render trigger buttons that call TdModal API.
    - Stories:
      1. **SimpleModal** — Button that calls `TdModal.show({title: 'Thong bao', body: '<p>Noi dung modal</p>'})`
      2. **ConfirmDialog** — Button that calls `TdModal.confirm({title: 'Xac nhan', message: 'Ban co chac chan?'})` and shows result
      3. **SuccessDialog** — Button that calls `TdModal.success({title: 'Thanh cong', message: 'Da luu thanh cong'})`
      4. **LoadingModal** — Button that shows loading modal for 2 seconds then closes
      5. **StackedModals** — Button that opens 2 modals stacked (composition example per D-06)
    - Import TdModal at top. Each story renders a button with onclick in play function.
    - Pattern: render returns a button, play function adds click handler that calls TdModal API.

    **td-toast.stories.js** — Title: 'Feedback/Toast'
    - Static-class toasts (TdToast.success(), etc.)
    - Stories:
      1. **AllVariants** — 4 buttons: Success, Error, Warning, Info. Each calls TdToast.[variant]('message')
      2. **Success** — Single button calling TdToast.success('Luu thanh cong!')
      3. **Error** — TdToast.error('Co loi xay ra!')
      4. **Warning** — TdToast.warning('Canh bao!')
      5. **AutoDismiss** — Shows toast with custom duration
    - Import TdToast at top.

    **td-tooltip.stories.js** — Title: 'Feedback/Tooltip'
    - TdTooltip uses data-tooltip attribute on any element.
    - Stories:
      1. **Default** — Button with `data-tooltip="Tooltip text"`. Import tooltip module to auto-init.
      2. **Positions** — 4 buttons positioned to demonstrate auto-positioning (top, bottom)
      3. **CustomColor** — `data-tooltip="Custom" data-tooltip-color="#8b5cf6"`
      4. **OnLinks** — Tooltips on `<a>` tags and inline elements
    - Import `'../../src/feedback/td-tooltip.js'` to trigger auto-init singleton.

    **td-loading.stories.js** — Title: 'Feedback/Loading'
    - Static TdLoading + factory TdLoadingSpinner
    - Stories:
      1. **FullscreenOverlay** — Button that calls `TdLoading.show('Dang tai...')` then hides after 2s
      2. **InlineSpinner** — Render multiple TdLoadingSpinner.create() results with different sizes
      3. **CustomMessage** — Button with custom loading message
      4. **WithAutoHide** — Shows loading with maxDuration=3000
    - Import TdLoading, TdLoadingSpinner at top.

    All stories: tags: ['autodocs'], use play functions for interactive setup.
  </action>
  <verify>
    <automated>cd /Users/theduyet/Documents/Code/components && for f in src/feedback/td-modal.stories.js src/feedback/td-toast.stories.js src/feedback/td-tooltip.stories.js src/feedback/td-loading.stories.js; do echo "$f:"; grep -c "export const" "$f"; done</automated>
  </verify>
  <acceptance_criteria>
    - grep "title: 'Feedback/Modal'" src/feedback/td-modal.stories.js
    - grep "title: 'Feedback/Toast'" src/feedback/td-toast.stories.js
    - grep "title: 'Feedback/Tooltip'" src/feedback/td-tooltip.stories.js
    - grep "title: 'Feedback/Loading'" src/feedback/td-loading.stories.js
    - grep "TdModal" src/feedback/td-modal.stories.js (imports API)
    - grep "TdToast" src/feedback/td-toast.stories.js (imports API)
    - grep "data-tooltip" src/feedback/td-tooltip.stories.js (tooltip attribute)
    - grep "TdLoadingSpinner" src/feedback/td-loading.stories.js (inline spinner)
    - Each file has at least 3 story exports
  </acceptance_criteria>
  <done>All 4 feedback components have Storybook stories with 3+ variants each, showing all APIs and interaction patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Wire package exports and index.js for display components</name>
  <files>package.json, index.js</files>
  <read_first>
    - /Users/theduyet/Documents/Code/components/package.json (current exports)
    - /Users/theduyet/Documents/Code/components/index.js (current re-exports)
  </read_first>
  <action>
    Add display component exports to package.json and index.js. This is the final wiring step — after this, consumers can `import 'td-components/table'`.

    **package.json exports** — Add these entries to the existing `"exports"` object:
    ```
    "./table": "./src/display/td-table.js",
    "./tabs": "./src/display/td-tabs.js",
    "./pagination": "./src/display/td-pagination.js",
    "./empty-state": "./src/display/td-empty-state.js"
    ```

    **index.js** — Add these re-exports at the end:
    ```
    export { TdTable } from './src/display/td-table.js';
    export { TdTabs } from './src/display/td-tabs.js';
    export { TdPagination } from './src/display/td-pagination.js';
    export { TdEmptyState } from './src/display/td-empty-state.js';
    ```

    Verify the full exports list is correct — no duplicates, all components covered.
  </action>
  <verify>
    <automated>cd /Users/theduyet/Documents/Code/components && node -e "import('./index.js').then(m => { const exports = Object.keys(m); console.log('Total exports:', exports.length); console.log('Has TdTable:', exports.includes('TdTable')); console.log('Has TdTabs:', exports.includes('TdTabs')); console.log('Has TdPagination:', exports.includes('TdPagination')); console.log('Has TdEmptyState:', exports.includes('TdEmptyState')); })"</automated>
  </verify>
  <acceptance_criteria>
    - grep "./table" package.json
    - grep "./tabs" package.json
    - grep "./pagination" package.json
    - grep "./empty-state" package.json
    - grep "TdTable" index.js
    - grep "TdTabs" index.js
    - grep "TdPagination" index.js
    - grep "TdEmptyState" index.js
  </acceptance_criteria>
  <done>All 4 display components have granular export paths in package.json and barrel re-exports in index.js. Library is complete.</done>
</task>

<task type="auto">
  <name>Task 3: Create composition story</name>
  <files>src/display/td-composition.stories.js</files>
  <read_first>
    - /Users/theduyet/Documents/Code/components/src/display/td-table.js
    - /Users/theduyet/Documents/Code/components/src/feedback/td-modal.js
    - /Users/theduyet/Documents/Code/components/src/feedback/td-toast.js
  </read_first>
  <action>
    Create composition stories combining multiple components (per D-06).

    **td-composition.stories.js** — Title: 'Examples/Composition'
    - tags: ['autodocs']

    Stories:
    1. **TableWithActions** — Table where a column's render function creates action buttons. Clicking "Delete" shows TdModal.confirm(), clicking "View" shows TdModal.show() with row details. On confirm, TdToast.success('Da xoa!').
    2. **FormAndFeedback** — A form-like layout with td-input-field, td-dropdown, td-button. On submit button click, show TdLoading for 1s, then TdToast.success. Demonstrates cross-category composition.

    Import all needed components at top. Use play functions to wire up interactive behavior.

    These are primarily demonstration stories — showing how components work together in real scenarios.
  </action>
  <verify>
    <automated>cd /Users/theduyet/Documents/Code/components && grep -c "export const" src/display/td-composition.stories.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "title: 'Examples/Composition'" src/display/td-composition.stories.js
    - grep "TdModal" src/display/td-composition.stories.js (uses modal)
    - grep "TdToast" src/display/td-composition.stories.js (uses toast)
    - grep "td-table" src/display/td-composition.stories.js (uses table)
    - At least 2 story exports
  </acceptance_criteria>
  <done>Composition stories demonstrate components working together: table with modal/toast actions, form with loading/feedback. Real-world usage patterns.</done>
</task>

</tasks>

<verification>
- All feedback components now have stories (Modal, Toast, Tooltip, Loading)
- All display components have stories (from Plans 01-03)
- Composition stories show cross-component interaction
- Package exports cover all components
- index.js barrel exports cover all components
- Total: every component in the library has Storybook coverage
</verification>

<success_criteria>
- Every component has a Storybook story with controls and multiple variants
- At least one composition story combines components from different categories
- Package exports include all display components (./table, ./tabs, ./pagination, ./empty-state)
- index.js exports all display components
- DX-02 requirement fully satisfied — complete Storybook coverage
</success_criteria>

<output>
After completion, create `.planning/phases/04-display-stories/04-04-SUMMARY.md`
</output>
