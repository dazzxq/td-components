---
phase: 05-tailwind-v4-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/base/sample/td-sample.js
  - src/form/td-datetime-picker.js
  - src/display/td-table.js
  - tailwind.config.js
  - package.json
autonomous: true
requirements:
  - TW4-01
  - TW4-02
  - TW4-03
must_haves:
  truths:
    - "All component Tailwind classes produce the same visual output under v4 as they did under v3"
    - "No v3 config remnants exist in the project"
    - "package.json peerDependencies require Tailwind v4+"
  artifacts:
    - path: "src/base/sample/td-sample.js"
      provides: "Sample component with v4-compatible classes"
      contains: "shadow-xs"
    - path: "src/form/td-datetime-picker.js"
      provides: "DateTime picker with v4-compatible classes"
      contains: "rounded-sm"
    - path: "src/display/td-table.js"
      provides: "Table component with v4-compatible classes"
      contains: "rounded-sm"
    - path: "package.json"
      provides: "Updated peerDependencies and cleaned devDependencies"
      contains: ">=4.0.0"
  key_links:
    - from: "package.json"
      to: "tailwindcss"
      via: "peerDependencies"
      pattern: ">=4\\.0\\.0"
---

<objective>
Fix renamed Tailwind v4 utility classes across all component files, delete the v3 tailwind.config.js, remove autoprefixer from devDependencies, and update peerDependencies to require Tailwind v4+.

Purpose: Complete the core Tailwind v3-to-v4 migration so all components render correctly under v4 with no v3 remnants.
Output: Updated component files, deleted config, cleaned package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-i-sang-tailwind-v4-cho-t-i-i-m-ko-l-m-break-m-i-th/05-CONTEXT.md
@.planning/phases/05-i-sang-tailwind-v4-cho-t-i-i-m-ko-l-m-break-m-i-th/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix renamed Tailwind v4 utility classes in component files</name>
  <files>src/base/sample/td-sample.js, src/form/td-datetime-picker.js, src/display/td-table.js</files>
  <read_first>
    - src/base/sample/td-sample.js (current class usage on lines 23, 27)
    - src/form/td-datetime-picker.js (current class usage on lines 205, 210, 215, 218, 254)
    - src/display/td-table.js (current class usage on lines 227, 235, 236, 247)
    - .planning/phases/05-i-sang-tailwind-v4-cho-t-i-i-m-ko-l-m-break-m-i-th/05-RESEARCH.md (utility class audit table)
  </read_first>
  <action>
Per D-04 and D-05, the following classes need updating to preserve v3 visual output under v4. Per D-04, `ring-offset-1` in td-button.js and td-empty-state.js stays as-is.

**src/base/sample/td-sample.js:**
- Line 23: Change `border rounded-lg shadow-sm` to `border border-gray-200 rounded-lg shadow-xs`
  - `border` alone now uses currentColor in v4 (was gray-200 in v3) — add explicit `border-gray-200`
  - `shadow-sm` scale shifted in v4 — `shadow-xs` is the v4 equivalent of v3's `shadow-sm`
- Line 27: Change `rounded` to `rounded-sm`
  - v3 `rounded` was 0.25rem; v4 `rounded` is 0.125rem; `rounded-sm` restores 0.25rem

**src/form/td-datetime-picker.js:**
- Lines 205, 210, 215: Change `rounded` to `rounded-sm` in the time input fields
  - Each line has class containing `border border-gray-300 rounded text-sm` — change `rounded` to `rounded-sm`
- Line 218: Change `rounded` to `rounded-sm` in the error div
  - Class contains `bg-red-100 border border-red-200 rounded text-xs` — change `rounded` to `rounded-sm`
- Line 254: Change `rounded` to `rounded-sm` in the preview div
  - Class contains `bg-gray-50 border border-gray-200 rounded text-center` — change `rounded` to `rounded-sm`

**src/display/td-table.js:**
- Line 227: Change `rounded` to `rounded-sm` in skeleton cell
  - Class contains `h-4 bg-gray-200 rounded animate-pulse` — change `rounded` to `rounded-sm`
- Lines 235, 236: Change `rounded` to `rounded-sm` in skeleton header elements
  - Class contains `bg-gray-200 rounded animate-pulse` — change `rounded` to `rounded-sm`
- Line 247: Change `rounded` to `rounded-sm` in skeleton footer element
  - Class contains `h-8 w-64 bg-gray-200 rounded animate-pulse` — change `rounded` to `rounded-sm`

Total changes: 10 instances of `rounded` -> `rounded-sm`, 1 instance of `shadow-sm` -> `shadow-xs`, 1 instance adding `border-gray-200`.
  </action>
  <verify>
    <automated>grep -c "rounded-sm" src/form/td-datetime-picker.js | grep -q "5" && grep -c "rounded-sm" src/display/td-table.js | grep -q "4" && grep -c "rounded-sm" src/base/sample/td-sample.js | grep -q "1" && grep -q "shadow-xs" src/base/sample/td-sample.js && grep -q "border-gray-200" src/base/sample/td-sample.js && ! grep -P " rounded " src/base/sample/td-sample.js && ! grep -P " rounded " src/form/td-datetime-picker.js && ! grep -P " rounded " src/display/td-table.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/base/sample/td-sample.js line 23 contains `border border-gray-200 rounded-lg shadow-xs`
    - src/base/sample/td-sample.js line 27 contains `rounded-sm` instead of bare `rounded`
    - src/form/td-datetime-picker.js contains 5 instances of `rounded-sm` (lines 205, 210, 215, 218, 254)
    - src/form/td-datetime-picker.js contains 0 instances of bare ` rounded ` (space-delimited)
    - src/display/td-table.js contains 4 instances of `rounded-sm` (lines 227, 235, 236, 247)
    - src/display/td-table.js contains 0 instances of bare ` rounded ` (space-delimited)
    - No file contains bare `shadow-sm` class (only `shadow-xs`)
    - ring-offset-1 in td-button.js and td-empty-state.js remains UNCHANGED (per D-04)
  </acceptance_criteria>
  <done>All 12 class changes applied. Components will render identically under v4 as they did under v3.</done>
</task>

<task type="auto">
  <name>Task 2: Delete v3 config, clean devDependencies, update peerDependencies</name>
  <files>tailwind.config.js, package.json</files>
  <read_first>
    - tailwind.config.js (file to delete — confirm it is the v3 config with content array)
    - package.json (current peerDependencies and devDependencies)
    - postcss.config.js (confirm it does NOT reference autoprefixer — it should only have @tailwindcss/postcss)
  </read_first>
  <action>
Per D-01: Delete `tailwind.config.js` entirely. The file contains a v3-format config with `content`, `theme`, `plugins` keys. v4 does not use JS config files — the project already has `@import "tailwindcss"` in CSS and `@tailwindcss/postcss` in PostCSS config.

Per D-06: Update `package.json` peerDependencies:
- Change `"tailwindcss": ">=3.0.0"` to `"tailwindcss": ">=4.0.0"`

Per Claude's discretion (autoprefixer cleanup):
- Remove `"autoprefixer": "^10.4.27"` from devDependencies — Tailwind v4 includes autoprefixer built-in
- Verify postcss.config.js does NOT reference autoprefixer (it does not — only `@tailwindcss/postcss`)

Per D-02: Do NOT modify `postcss.config.js` — it already uses the correct v4 format.
Per D-03: Do NOT add any `@source` directive to `src/styles/tailwind.css` — v4 auto-detects content.

After package.json changes, run `npm install` to update the lockfile.

After deletion, verify no other file in the project references `tailwind.config` (except planning docs and README which will be updated in Plan 02).
  </action>
  <verify>
    <automated>! test -f tailwind.config.js && node -e "import('./package.json', {assert:{type:'json'}}).then(p => { if(p.default.peerDependencies.tailwindcss !== '>=4.0.0') throw 'peer wrong'; if(p.default.devDependencies.autoprefixer) throw 'autoprefixer still present'; console.log('PASS') })"</automated>
  </verify>
  <acceptance_criteria>
    - tailwind.config.js does NOT exist (deleted)
    - package.json peerDependencies contains `"tailwindcss": ">=4.0.0"`
    - package.json devDependencies does NOT contain `autoprefixer`
    - postcss.config.js is unchanged (still contains only `@tailwindcss/postcss`)
    - src/styles/tailwind.css is unchanged (still contains only `@import "tailwindcss"`)
  </acceptance_criteria>
  <done>v3 config deleted, autoprefixer removed, peerDependencies updated to v4. No v3 remnants in project config.</done>
</task>

</tasks>

<verification>
1. `! test -f tailwind.config.js` — v3 config file deleted
2. `grep -r "shadow-sm" src/ --include="*.js" | grep -v node_modules` — no bare shadow-sm remaining
3. `grep -rP " rounded " src/ --include="*.js" | grep -v node_modules` — no bare rounded remaining
4. `node -e "import('./package.json', {assert:{type:'json'}}).then(p => console.log(p.default.peerDependencies))"` — shows `>=4.0.0`
5. `grep -q "ring-offset-1" src/form/td-button.js && grep -q "ring-offset-1" src/display/td-empty-state.js && echo "ring-offset preserved"` — D-04 honored
</verification>

<success_criteria>
- All 10 `rounded` -> `rounded-sm` replacements applied across 3 files
- 1 `shadow-sm` -> `shadow-xs` replacement in td-sample.js
- 1 `border-gray-200` addition in td-sample.js
- tailwind.config.js deleted
- autoprefixer removed from devDependencies
- peerDependencies updated to `>=4.0.0`
- ring-offset-1 unchanged in td-button.js and td-empty-state.js (per D-04)
</success_criteria>

<output>
After completion, create `.planning/phases/05-i-sang-tailwind-v4-cho-t-i-i-m-ko-l-m-break-m-i-th/05-01-SUMMARY.md`
</output>
