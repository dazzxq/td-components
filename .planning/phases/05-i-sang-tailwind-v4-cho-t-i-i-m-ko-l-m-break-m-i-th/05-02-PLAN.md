---
phase: 05-tailwind-v4-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - README.md
autonomous: false
requirements:
  - TW4-04
  - TW4-05
must_haves:
  truths:
    - "README documents v4 consumer setup with @source directive"
    - "All components render correctly in Storybook after migration"
    - "Storybook builds without errors"
  artifacts:
    - path: "README.md"
      provides: "Updated consumer documentation for Tailwind v4"
      contains: "@source"
  key_links:
    - from: "README.md"
      to: "consumer project"
      via: "setup instructions"
      pattern: "@tailwindcss/postcss"
---

<objective>
Update README.md with Tailwind v4 consumer setup instructions and visually verify all components in Storybook.

Purpose: Ensure consumers know how to configure their projects for v4, and confirm the migration didn't break any component visuals.
Output: Updated README.md, verified Storybook renders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-i-sang-tailwind-v4-cho-t-i-i-m-ko-l-m-break-m-i-th/05-CONTEXT.md
@.planning/phases/05-i-sang-tailwind-v4-cho-t-i-i-m-ko-l-m-break-m-i-th/05-RESEARCH.md
@.planning/phases/05-i-sang-tailwind-v4-cho-t-i-i-m-ko-l-m-break-m-i-th/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README with Tailwind v4 consumer setup</name>
  <files>README.md</files>
  <read_first>
    - README.md (current content — the Tailwind Configuration section references v3 tailwind.config.js)
    - .planning/phases/05-i-sang-tailwind-v4-cho-t-i-i-m-ko-l-m-break-m-i-th/05-RESEARCH.md (consumer setup examples and @source directive info)
  </read_first>
  <action>
Per D-07: Update the "Tailwind Configuration" section in README.md. Replace the entire section (lines 13-25) with v4 setup instructions.

Replace this v3 content:
```
## Tailwind Configuration

Add the component source files to your `tailwind.config.js` content array. This is **required** -- without it, Tailwind will purge the CSS classes used by components and they will render without styles.

\`\`\`js
// tailwind.config.js
export default {
  content: [
    // ... your project files
    './node_modules/@dazzxq/td-components/src/**/*.js',
  ],
};
\`\`\`
```

With this v4 content:
```
## Tailwind Configuration (v4)

This library requires **Tailwind CSS v4+**. Add the component source to your CSS so Tailwind scans component classes (v4 excludes `node_modules` by default).

\`\`\`css
/* your main CSS file */
@import "tailwindcss";
@source "../node_modules/@dazzxq/td-components/src";
\`\`\`

Your PostCSS config should use the v4 plugin:

\`\`\`js
// postcss.config.js
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
\`\`\`

> **Migrating from v3?** Replace your `tailwind.config.js` content array with the `@source` directive above. See the [Tailwind v4 upgrade guide](https://tailwindcss.com/docs/upgrade-guide) for details.
```

Keep all other README sections unchanged.
  </action>
  <verify>
    <automated>grep -q "@source" README.md && grep -q "@tailwindcss/postcss" README.md && grep -q "Tailwind CSS v4" README.md && ! grep -q "tailwind.config.js" README.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `@source "../node_modules/@dazzxq/td-components/src"`
    - README.md contains `@tailwindcss/postcss` in PostCSS config example
    - README.md contains `Tailwind CSS v4` text
    - README.md does NOT contain `tailwind.config.js` anywhere
    - README.md still contains all other sections: Install, Usage, Creating Components, API Reference, Development, License
  </acceptance_criteria>
  <done>README updated with v4 consumer instructions including @source directive and PostCSS config example.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all components in Storybook</name>
  <files>none</files>
  <action>
Run `npm run storybook` to start the development server. Then run `npm run build-storybook` to verify the static build completes without errors. Present the Storybook URL to the user for visual verification of all components after the Tailwind v4 migration.
  </action>
  <what-built>
    Tailwind v4 migration complete:
    - Fixed 12 renamed utility classes across 3 component files (rounded -> rounded-sm, shadow-sm -> shadow-xs, added border-gray-200)
    - Deleted v3 tailwind.config.js
    - Removed autoprefixer from devDependencies
    - Updated peerDependencies to >=4.0.0
    - Updated README with v4 consumer setup
  </what-built>
  <how-to-verify>
    1. Run `npm run storybook` (should start without errors)
    2. Visit http://localhost:6006
    3. Check these components for visual correctness:
       - **Sample** (Base/Sample): Border should be light gray (not dark/currentColor), shadow should be subtle, button should have rounded corners
       - **DateTime Picker** (Form/DateTime Picker): Time input fields should have rounded corners, error state should have rounded red border
       - **Table** (Display/Table): Loading skeleton should show rounded pulse placeholders
       - **Button** (Form/Button): Focus state should show ring offset (ring-offset-1 unchanged)
       - **Empty State** (Display/Empty State): Action buttons should show ring offset on focus
    4. Spot-check 2-3 other components to confirm no visual regression
    5. Run `npm run build-storybook` to confirm static build works
  </how-to-verify>
  <verify>
    <automated>npm run build-storybook 2>&1 | tail -5</automated>
  </verify>
  <done>User has visually verified all components render correctly in Storybook after Tailwind v4 migration. Storybook build succeeds.</done>
  <resume-signal>Type "approved" if all components look correct, or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
1. `grep -q "@source" README.md` — v4 source directive documented
2. `! grep -q "tailwind.config.js" README.md` — no v3 references in docs
3. Storybook starts and renders all components (human verified)
4. `npm run build-storybook` completes without errors
</verification>

<success_criteria>
- README Tailwind section updated with v4 @source directive and PostCSS config
- No references to tailwind.config.js in README
- All components render correctly in Storybook (human verified)
- Storybook build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-i-sang-tailwind-v4-cho-t-i-i-m-ko-l-m-break-m-i-th/05-02-SUMMARY.md`
</output>
