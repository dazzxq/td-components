---
phase: 03-feedback-overlay
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/feedback/td-toast.js
  - src/feedback/td-loading.js
autonomous: true
requirements: [FEED-03, FEED-05]

must_haves:
  truths:
    - "TdToast.success('Done') shows a green toast in top-right corner"
    - "TdToast.error('Failed') shows a red toast with longer duration"
    - "Maximum 5 toasts visible, oldest removed via FIFO when exceeded"
    - "Toasts auto-dismiss after configurable duration and can be clicked to dismiss"
    - "TdLoading.show('Loading...') shows fullscreen overlay with circular spinner"
    - "TdLoading.hide() removes the overlay"
    - "TdLoading auto-hides after maxDuration (default 30s)"
    - "TdLoadingSpinner.create({size}) returns inline spinner element"
  artifacts:
    - path: "src/feedback/td-toast.js"
      provides: "Toast notifications with 4 variants"
      exports: ["TdToast"]
    - path: "src/feedback/td-loading.js"
      provides: "Fullscreen loading overlay and inline spinner"
      exports: ["TdLoading", "TdLoadingSpinner"]
  key_links:
    - from: "TdToast.show()"
      to: "document.body"
      via: "auto-creates container div, appends toast elements"
      pattern: "document\\.body\\.appendChild"
    - from: "TdToast.getToastZIndex()"
      to: "TdModalStackManager"
      via: "import to read modal stack for dynamic z-index"
      pattern: "import.*TdModalStackManager"
    - from: "TdLoading.show()"
      to: "document.body"
      via: "creates overlay element on first call"
      pattern: "document\\.body\\.appendChild"
---

<objective>
Port Toast and Loading components from DCMS to td-components as standalone JS utility classes.

Purpose: Toast provides user feedback notifications, Loading provides blocking/non-blocking loading indicators. Both use static method APIs similar to Modal.

Output: `src/feedback/td-toast.js` and `src/feedback/td-loading.js`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-feedback-overlay/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port TdToast</name>
  <files>src/feedback/td-toast.js</files>
  <read_first>
    - /Users/theduyet/Documents/Code/dcms2/resources/js/components/dcms-toast.js
    - src/feedback/td-modal-stack.js (for z-index reference — may not exist yet if Wave 1 parallel, handle gracefully)
  </read_first>
  <action>
    Create `src/feedback/td-toast.js` — port from DCMS `Toast` to `TdToast`.

    Standalone utility class with static methods (does NOT extend TdBaseElement per architecture note).

    Port these features from DCMS source:

    **Static state:**
    - `container = null` — toast container element
    - `TOAST_Z_INDEX_BASE = 99999` — always above modal stack
    - `MAX_VISIBLE = 5` — per D-08
    - `_activeToasts = []`

    **Methods (per D-06, D-07, D-08, D-09):**
    1. **ensureContainer()** — per D-07: Auto-create container on first call. Create div with `id="td-toast-container"`, position fixed top-right (`top-20 right-4`), flex column, pointer-events-none. Update z-index dynamically per D-09.
    2. **getToastZIndex()** — per D-09: Check if TdModalStackManager exists and has modals, compute z-index = max modal z-index + 1000. Fallback to TOAST_Z_INDEX_BASE. Import TdModalStackManager but handle gracefully if stack manager is not loaded (when running in parallel Wave 1).
    3. **show(message, type, duration)** — Create toast element, apply theme colors, slide-in animation (translate-x-4 opacity-0 -> translate-x-0 opacity-100 via rAF), set auto-dismiss timeout, bind click to dismiss. Per D-08: FIFO eviction when > MAX_VISIBLE.
    4. **success(message, duration=4000)** — Per D-06: Static method, green theme
    5. **error(message, duration=5000)** — Per D-06: Static method, red theme, longer default
    6. **warning(message, duration=4000)** — Per D-06: Static method, yellow theme
    7. **info(message, duration=4000)** — Per D-06: Static method, blue theme

    **Theme mapping** (from DCMS):
    - success: bg-green-500/85, checkmark icon SVG
    - error: bg-red-500/85, X icon SVG
    - warning: bg-yellow-500/85, triangle exclamation icon SVG
    - info: bg-blue-500/85, info circle icon SVG

    **Toast element structure** (from DCMS):
    - Outer div: toast-item class, pointer-events-auto, cursor-pointer, transform transition
    - Inner div: px-4 py-3 rounded-xl text-white, theme bg, glass effect (backdrop-filter blur)
    - aria attributes: role="alert", aria-live (assertive for error, polite for others)

    **Dismiss behavior:**
    - Click anywhere on toast to dismiss
    - Auto-dismiss after duration (0 = no auto-dismiss)
    - Dismiss animation: slide right + fade out, remove DOM after 180ms
    - Guard against double-removal with `_removed` flag

    Rename container id to `td-toast-container`. Export as named export `TdToast`.

    **Graceful TdModalStackManager import:** Use dynamic import or try/catch. If modal-stack module fails to load (parallel execution), fall back to TOAST_Z_INDEX_BASE. Simplest approach: import at top level, if TdModalStackManager.stack is undefined/empty, use base z-index.
  </action>
  <verify>
    <automated>grep -c "static show\|static success\|static error\|static warning\|static info\|static ensureContainer\|static getToastZIndex" src/feedback/td-toast.js | grep -q "7"</automated>
  </verify>
  <acceptance_criteria>
    - File exports `TdToast` class
    - Has 7 static methods: show, success, error, warning, info, ensureContainer, getToastZIndex
    - success/error/warning/info call show() with correct type and duration
    - ensureContainer() creates container div appended to body (per D-07)
    - MAX_VISIBLE = 5 with FIFO eviction (per D-08)
    - Toast z-index dynamically above modal stack (per D-09)
    - Toast has role="alert" and aria-live attribute
    - Click-to-dismiss and auto-dismiss with animation
    - Container id is "td-toast-container"
    - Does NOT extend TdBaseElement
  </acceptance_criteria>
  <done>TdToast exported with 4 variant methods, auto-create container, FIFO eviction at 5 max, dynamic z-index above modals, slide-in/out animation</done>
</task>

<task type="auto">
  <name>Task 2: Port TdLoading and TdLoadingSpinner</name>
  <files>src/feedback/td-loading.js</files>
  <read_first>
    - /Users/theduyet/Documents/Code/dcms2/resources/js/components/dcms-loading.js
  </read_first>
  <action>
    Create `src/feedback/td-loading.js` — port both `Loading` and `LoadingSpinner` from DCMS.

    Two standalone utility classes (do NOT extend TdBaseElement per architecture note).

    **TdLoading — Fullscreen overlay (per D-13, D-14):**

    Static state:
    - `element = null` — overlay DOM element
    - `_maxDurationTimer = null`

    Methods:
    1. **init()** — Create fullscreen overlay: fixed inset-0, z-[99999], hidden by default, dark backdrop (bg rgba(0,0,0,0.25)). Inner card with Google-style circular SVG spinner. Include `<style>` tag with keyframe animations for spinner rotation and dash animation (from DCMS). Card uses solid white bg (not glass — avoids backdrop-filter conflicts per DCMS source). Append to body.
    2. **show(messageOrOptions)** — per D-14: Accept string or `{message, maxDuration}`. Default message 'Dang tai...', default maxDuration 30000. Init if needed, set message text, remove 'hidden' class, start maxDuration auto-hide timer.
    3. **hide()** — Clear maxDuration timer, add 'hidden' class.
    4. **wrap(asyncFn, message)** — Async wrapper: show -> await fn -> hide (in finally block).

    **Spinner styles** (from DCMS — embed in `<style>` tag inside overlay):
    - `.td-loading-card` — flex column, centered, padding 32px 44px, rounded-[20px], solid white bg, shadow
    - `.td-circular-spinner` — 56x56px, rotate animation 1.4s linear infinite
    - `.td-spinner-track` — circle, rgba(59,130,246,0.15) stroke
    - `.td-spinner-arc` — circle, #3b82f6 stroke, dash animation 1.4s ease-in-out infinite
    - `@keyframes td-spinner-rotate` and `td-spinner-dash` — port from DCMS
    - `@media (prefers-reduced-motion: reduce)` — slower rotation, no dash animation
    - `.td-loading-message` — 15px, font-weight 500, #374151

    Rename all CSS classes from `dcms-` to `td-` prefix. Overlay id: `td-loading`.

    **TdLoadingSpinner — Inline spinner:**

    Static method:
    1. **create(options)** — Returns HTMLElement. Options: `{ size: 'sm'|'md'|'lg', color, trackColor, className }`. Size mapping: sm=20px/3, md=32px/4, lg=48px/5. Creates div with SVG spinner, injects keyframe styles once (check for existing `#td-spinner-keyframes` style element). Uses `td-spinner` class, `td-inline-spinner-rotate` and `td-inline-spinner-dash` keyframe names.

    Export both as named exports: `TdLoading`, `TdLoadingSpinner`.
  </action>
  <verify>
    <automated>grep -c "static init\|static show\|static hide\|static wrap\|static create" src/feedback/td-loading.js | grep -q "5"</automated>
  </verify>
  <acceptance_criteria>
    - File exports both `TdLoading` and `TdLoadingSpinner`
    - TdLoading has 4 static methods: init, show, hide, wrap
    - TdLoadingSpinner has 1 static method: create
    - show() accepts string or {message, maxDuration} (per D-14)
    - maxDuration default 30000ms, auto-hides with console.warn
    - wrap() shows loading, awaits async fn, hides in finally
    - Spinner uses Google-style SVG with rotate + dash keyframe animations
    - @media prefers-reduced-motion handled
    - All CSS classes use td- prefix (not dcms-)
    - Overlay id is "td-loading"
    - Does NOT extend TdBaseElement
  </acceptance_criteria>
  <done>TdLoading and TdLoadingSpinner exported — fullscreen overlay with Google SVG spinner, maxDuration auto-hide, inline spinner factory, reduced-motion support</done>
</task>

</tasks>

<verification>
- `grep "export.*TdToast" src/feedback/td-toast.js` returns match
- `grep "export.*TdLoading" src/feedback/td-loading.js` returns match
- `grep "export.*TdLoadingSpinner" src/feedback/td-loading.js` returns match
- `grep "MAX_VISIBLE.*5" src/feedback/td-toast.js` returns match
- `grep "maxDuration\|_maxDurationTimer" src/feedback/td-loading.js` returns match
- `grep "prefers-reduced-motion" src/feedback/td-loading.js` returns match
</verification>

<success_criteria>
- TdToast.success('msg') creates a visible green toast in top-right, auto-dismisses after 4s
- TdToast enforces max 5 visible with FIFO eviction
- TdLoading.show() displays fullscreen overlay with circular spinner
- TdLoading.hide() removes overlay, TdLoading.wrap(fn) wraps async operations
- TdLoadingSpinner.create({size:'sm'}) returns inline spinner element
- Both classes are fully self-contained, no TdBaseElement dependency
</success_criteria>

<output>
After completion, create `.planning/phases/03-feedback-overlay/03-02-SUMMARY.md`
</output>
